# Allow hal_camera_default to read camera_persist_file
r_dir_file(hal_camera_default, camera_persist_file)

# Allow camera HAL to write camera_persist_file
allow hal_camera_default camera_persist_file:file w_file_perms;

# Allow camera HAL to read auxiliary camera properties
get_prop(hal_camera_default, vendor_persist_camera_prop)
